{
    "name": "huntersnotes",
    "version": "0.0.1",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/inuEbisu/HuntersNotes.git"
    },
    "scripts": {
        "build": "python scripts/update_site_stats.py && zensical build --clean",
        "serve": "python scripts/update_site_stats.py && zensical serve",
        "format": "prettier --write .",
        "format:check": "prettier --check ."
    },
    "devDependencies": {
        "prettier": "^3.6.2"
    }
}
